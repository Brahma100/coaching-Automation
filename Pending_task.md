If you want, I can also make Send Reminder perform a real backend reminder action directly from this modal (instead of routing to Actions), but that needs a dedicated API for session/batch reminder dispatch.

